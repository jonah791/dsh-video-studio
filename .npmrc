# 禁止 npm 自动安装 peerDependencies（npm 7+ 默认行为）。
#
# 原因（dsh-plugin-pitfalls 技能）：
# peerDependencies 被自动安装会在插件内生成 @deepseek-ai/* 的**真实副本**，
# 而宿主用的是共享**链接层**（同一份代码）。一份代码两个实例
# → 跨实例 instanceof / WeakMap / 品牌校验失败（Typert remote 404 类故障）。
#
# 正解：本地只装 devDependencies（typescript / @types/node，纯类型无运行时），
# 运行时依赖靠 Node 解析向上走到宿主的链接层。
legacy-peer-deps=true
